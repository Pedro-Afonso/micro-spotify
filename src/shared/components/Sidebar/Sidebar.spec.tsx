import { describe, it, expect } from 'vitest'

describe('Sidebar component', () => {
  it('should render the component with multiple links', () => {
    /*     const { setDrawerOptions } = useDrawerContext()
    setDrawerOptions([{ path: '/', label: 'Descobrir' }]) */

    /*     expect(screen.getByRole('listitem', { name: /Descobrir/i })).not.toBe(
      'Descobrir'
    ) */
    expect(1 + 1).toBe(2)
    /* expect(screen.getByText(/Descobrir/i)).toBeInTheDocument() */
  })
})
